// import { User } from "src/entity/User"
// import { isAuthenticated } from "src/middleware/Authenticate"

// export const async getUser= (token:string)=>{
//     if(isAuthenticated(token)){
//         return await User.find({where:{id:}})
//     }
// }